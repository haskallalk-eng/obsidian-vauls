---
date: 2026-04-27
tags:
  - daily
  - 2026/04
mood: focused
---

# 2026-04-27 · Montag

> [!tip] Haupt-Fokus
> Audit-Round-8 + Round-9 an einem Tag — fünf Codex-Cross-Review-Sessions insgesamt, ~17 Backlog-Findings gefixt + zwei Prod-Deploys. Round-8: Prompt-Injection-Quarantine + PII-Redaction + Admin-Audit-Log live. Round-9: Phone-Pool-Konsistenz inkl. autoProvision Pool-First + bundleSid (DACH-Compliance) + pg_advisory_xact_lock gegen Stripe-Webhook-Race + DB-vs-Twilio-Reverse-Diff. Codex hat in Round 8+9 dreimal vor Deploy gerettet (Regex zu breit, Cache-Invalidation-Lücke, Org-Race bei Stripe-Retry).

## ⚡ Done heute

### [[Phonbot/Overview|Phonbot]] — Audit-Round-10: erste bidirektionale Cross-Review (Codex authort, Claude reviewt)

- [x] **Bidirektionalität anerkannt** — Rounds 6-9 waren einseitig (Claude author → Codex review). Round 10 dreht das Pattern wie in `RULES.md` vorgesehen um. Codex implementiert NICE-2 Webhook-Health-Tracking als Author-Pass, Claude reviewt + entscheidet ob's so deployed wird.
- [x] **Codex's Implementation** (~530s, +257 LOC, 8 Files) — `inbound_webhook_health` Tabelle in `db.ts` (PRIMARY KEY (tenant_id, webhook_id), Partial-Index `(disabled_until) WHERE disabled_until IS NOT NULL`, 90d-Retention via `cleanupOldWebhookHealth`) + `upsertWebhookHealth(pool, params)` Helper mit TypeScript-discriminated-union (success/failure params, GREATEST-guard auf consecutive_failures gegen out-of-order-counter-decrease) + disabled-pre-check + success/failure-UPSERT in `inbound-webhooks.ts:fireInboundWebhooks` + `GET /agent-config/webhooks-health` Endpoint in `agent-config.ts` (Org-scope via `loadOwnedConfigRow`) + daily cleanup-cron in `index.ts` (240s post-startup, staggered). Konstanten `WEBHOOK_FAIL_THRESHOLD = 50` + `WEBHOOK_DISABLE_DURATION = '1 hour'`. Codex hat 3 Uncertainties + 1 Edge-Case selbst geflagged.
- [x] **Claude's Counter-Review** — 4 Findings:
  - **🟠 HIGH-A** (Codex Uncertainty #1 BESTÄTIGT): success-UPSERT setzt `disabled_until = NULL` unconditionally → Race wo eine späte 2xx-Response ein freshly-set disable wegwischt. Pre-Deploy-Fix in `db.ts:166-200`: success-Branch nutzt `CASE WHEN inbound_webhook_health.disabled_until > now() THEN inbound_webhook_health.disabled_until ELSE NULL END`. Live-Disable-Windows bleiben in Force; abgelaufene werden gecleart. Robuster als Codex's eigener `WHERE consecutive_failures > 0`-Vorschlag — schützt auch Out-of-Order-Arrivals.
  - **🔵 LOW-A** (Codex Uncertainty #3 BESTÄTIGT): GET-Endpoint zeigte orphan-rows von gelöschten Webhooks. Pre-Deploy-Fix: filter im Endpoint mit `liveWebhookIds`-Set aus `owned.data.inboundWebhooks`. DB-Rows bleiben für Audit (90d-Cleanup), API-Surface zeigt nur live state.
  - **🔵 LOW-B** (Codex Uncertainty #2 als Trade-off akzeptiert): extra DB-Roundtrip pro Webhook-fetch (~25 SELECTs/sec bei 5 events × 5 webhooks/sec). In-process-Cache mit kurzer TTL als Round-11-Optimierung dokumentiert.
  - **🔵 LOW-C** (Codex Edge-Case als bekannte Limitation): webhook-recreation mit gleicher ID erbt failure-state. UI-Workaround: neue UUID beim re-create. Permanenter Fix verlangt Diff zwischen alt/neu im writeConfig — größerer Scope-Change.
- [x] **Type-Check API + Web grün, prod-Deploy** — Container Healthy nach 21s. Tabelle `inbound_webhook_health` in prod via psql verifiziert. Commit `565e47d`.
- [x] **Cross-Review-Pattern bestätigt** — Codex hat ein gut-strukturiertes Feature implementiert + 4 Uncertainties selbst geflagged (höhere Self-Awareness als ich erwartet hätte). Claude hat den echten HIGH bestätigt + LOW-A gefixt + die 2 anderen als akzeptable Trade-offs verbucht. Beide Seiten haben Catch-fail-Punkte. Pattern für Round 11+: weiterhin abwechseln wo's Sinn macht; größere Items als Codex-Author-Task (hier 257 LOC in ~9min Implementation gut geklappt).
- [x] **Audit-Doc + INDEX aktualisiert** — Round-10-Update-Section in `07-tickets-insights-leads.md` mit Verdict-Tabelle, Findings-Severity-Tabelle, Cross-Review-Pattern-Note. Module 07 ist jetzt fast komplett geschlossen — nur HIGH-2 (Email-PII UX-Entscheid) + NICE-1 (crm_leads.email NOT NULL Datenmodell-Entscheid) offen.

### [[Phonbot/Overview|Phonbot]] — Audit-Round-9: Phone-Pool-Konsistenz + Retry-Suggestion-Dedup

- [x] **Codex-Plan-Review vor Start** — 5 Items klassifiziert (3 OK, 2 RISIKO, 1 SKIP). Reihenfolge `3 → 2 → 1 → 4` (risiko-aufsteigend). Item 5 (`inbound_webhooks_health`-Tabelle) als „eigenes Feature, nicht Audit-Fix" zurückgestellt.
- [x] **Item 3 — `/phone/forward` DB-touch entfernt** (M06-MEDIUM-X): INSERT INTO phone_numbers (method='forwarding') komplett raus. Endpoint behalten als read-only Carrier-Code-Helper-API. DACH-Whitelist auf customer-number bleibt (defense-in-depth obwohl kein Server-Dial mehr stattfindet). Codex bestätigt: kein anderer Code-Pfad las den `forwarding`-Row, dead code seit Round-6.
- [x] **Item 2 — DB-vs-Twilio-Reverse-Diff** (M06-MEDIUM-C): `syncTwilioNumbersToDb` mit Reverse-Diff-Loop. `limit:1000` (volle inventory). Pool-Rows >7 Tage alt + nicht in Twilio → DELETE; Assigned-Rows nie delete (UPDATE verified=false + log.error). Codex Code-Review fand fehlenden Cap → `MAX_POOL_DELETES_PER_RUN = 5` mit Warn-Log eingebaut bevor Deploy.
- [x] **Item 1 — autoProvisionGermanNumber Pool-First + Bundle** (M06-HIGH-2): Pool-Claim ZUERST mit gleicher CTE wie /phone/provision (`FOR UPDATE SKIP LOCKED`, Stale-PII-Reset im CTE). Bei Pool-Hit: Retell PATCH (provider_id gesetzt) oder IMPORT (NULL). Bei Retell-Fail: Pool-Rollback. Pool-Miss → Twilio-Buy mit `bundleSid + addressSid` (DACH-Compliance, fehlte vorher!), Loop über 5 Candidates. **Codex Code-Review fand HIGH-Bug**: existing-Check + Claim-Pipeline lief AUSSERHALB per-Org-Lock — zwei Stripe-Webhook-Retries hätten beide den Idempotency-Check passieren können → 2-3 Numbers pro Customer. **Pre-Deploy-Fix**: gesamte Body in `pg_advisory_xact_lock` per orgId-Hash gewrappt, Body in `runAutoProvisionBody` extrahiert für Lesbarkeit.
- [x] **Item 4 — Retry-Suggestion Embedding + Dedup** (M07-LOW-5): `checkFixEffectiveness` retry-INSERT bekommt embedding (try/catch fallback NULL bei Fail) + SELECT-then-INSERT Dedup auf `(org_id, status='pending', issue_summary)`. Codex empfahl Partial-Unique-Index als Round-10-Optimierung — akzeptiert weil fire-and-forget + Worst-Case 1 Duplicate-Row.
- [x] **Codex-Code-Review der Round-9-Diff** (476s, zweite Session) — 5 Items durchgegangen: Item 3 ✅ ack, Item 2 🟡 MEDIUM (delete-cap fehlt) → gefixt, Item 1 🔴 HIGH (Org-Race) → gefixt mit pg_advisory_xact_lock, Item 4 🔵 LOW (Race-Window) → akzeptiert, Item 5 (autoProvision Stale-PII-Reset) ✅ ack.
- [x] **Type-Check API + Web grün, prod-Deploy** — Container Healthy nach 18s. Commit `5587d7c`.
- [x] **Audit-Docs ergänzt** — Round-9-Update-Sektionen in `06-phone-twilio.md` + `07-tickets-insights-leads.md`. INDEX.md zeigt jetzt Module 06 + 07 fast komplett geschlossen (nur Module 06 MEDIUM-3 Twilio-Status-Webhook + Module 07 HIGH-2 Email-PII-UX-Entscheid + 1 NICE noch offen).

### [[Phonbot/Overview|Phonbot]] — Audit-Round-8: 13 Backlog-Findings aus Module 06+07 gefixt + deployed

- [x] **Codex-Plan-Review vor Start** (107s) — Reihenfolge `A → C → B` empfohlen (C bringt mehr Security pro Risiko-Einheit als B), Blocklist als Quarantine statt Drop, URL/Email-Detection raus (zu hohes False-Positive-Risiko). Plan-Review-Output strukturiert in 5 Bullet-Punkten zur jeder meiner Frage. Codex hat insbesondere darauf hingewiesen dass die Blocklist-Regex bei DE/EN-Phrasen wie „du bist jetzt …" Review-Last erzeugt + dass Bypass-Pfade beim Cache nur durch hard-required `writeConfig`-Diszipline geschlossen sind.
- [x] **Group A — Defensive Filters**: `classifyPromptFix()` Helper mit length-cap 500 + 8 narrow inject-Regex-Patterns DE/EN. Eingebaut an 4 INSERT-Sites in `insights.ts` (processIssue recurrence + new, checkFixEffectiveness retry, holisticReview). Apply-Route refused 409 wenn category=`prompt_injection_attempt`. Plus `redactPII(transcript)` in `retell-webhooks.ts` vor `INSERT INTO demo_calls` (90-Tage-Retention bleibt, aber redacted). Plus `redactPII()` auf `learning_corrections.original_text/corrected_text/correction_reason` in `loadRecentCorrectionsForFewShot` bevor's an OpenAI-Few-shot-Prompts geht (DSGVO Q5 closed). Plus `phone.ts:/phone/verify-forwarding` mit Redis-Counter `phone:verify:global:hourly`, hard cap 200/h env-tunable, return 503 + log.warn.
- [x] **Group C — Admin-Audit-Log**: Neue Tabelle `admin_read_audit_log (admin_email, route, params JSONB, result_count, ip)` mit 365-Tage-Retention via `cleanupOldAuditLogs()` daily cron. Per-route rate-limit 60/min auf `/admin/leads`, `/admin/leads/stats`, `/admin/demo-calls`. `recordAdminRead()` Helper als fire-and-forget INSERT mit `log.warn`-Fallback bei DB-Failure (Best-Effort, Sentry sieht Pattern via log).
- [x] **Group B — Caching + Backfill**: In-memory LRU für `(tenantId → InboundWebhookConfig[])` in `inbound-webhooks.ts`, TTL 60s, MAX 1000 entries, oldest-eviction. `invalidateInboundWebhooksCache()` exported, aufgerufen von `agent-config.ts:writeConfig` UND nach Codex-Q2-Review auch von `/agent-config/new` (sonst Lücke beim ersten Save). Plus neuer Endpoint `POST /insights/admin/embed-backfill` (platform-admin via `payload.admin`-Check), batches 50 Rows mit `WHERE embedding IS NULL`, returnt `{processed,failed,remaining}` für Polling.
- [x] **Codex-Code-Review der Round-8-Diff vor Deploy** (498s, zweite Session) — 5 Findings durchgegangen: Q1 Regex zu breit (`act as a` würde legitime „act as a phone agent" matchen) + Q2 Cache-Invalidation lückenhaft in `/agent-config/new` als ECHTE Bugs identifiziert; Q3 embed-backfill double-work + Q4 audit-log fire-and-forget als akzeptable Trade-offs bestätigt; Q5 LRU-Eviction-Race als KEIN Bug verifiziert (Node single-thread). Beide echten Bugs **vor Deploy gefixt**: Patterns auf 8 sehr enge Phrasen reduziert, `invalidateInboundWebhooksCache` in `/new` ergänzt.
- [x] **Type-Check API + Web grün, prod-Deploy** — `tsc --noEmit` für beide Pakete EXIT=0. SSH-Deploy auf 87.106.111.213, Container Healthy nach Boot, `[env] WARN: WEBHOOK_SIGNING_SECRET` erscheint wieder im Boot-Log (Round-7-Soft-Warn ist immer noch nicht hard-required gemacht — Reminder dass Op die Variable setzen muss). DB-Migration `admin_read_audit_log` in prod verifiziert via psql.
- [x] **Audit-Docs ergänzt** — Round-8-Update-Sektionen in `06-phone-twilio.md` + `07-tickets-insights-leads.md` mit ✅-Status pro Finding + Codex-Code-Review-Findings-Tabelle. INDEX.md aktualisiert.

### [[Phonbot/Overview|Phonbot]] — Vault-Logging + Hook-Deaktivierung beobachtet

- [x] **Codex/Vaso hat `import { checkForwardingVerificationMatch }` in `apps/api/src/retell-webhooks.ts` auskommentiert** — TODO-Kommentar im Code: „re-enable once phone.ts checkForwardingVerificationMatch is pushed. Currently disabled (with the call site below) so prod build doesn't fail on the missing export — Codex's helper exists locally but is uncommitted." Das heißt: der Loop-Test ist bis zum Commit/Push **funktional offline**, da der `call_started`-Webhook keinen Match auslöst. Der `/phone/verify-forwarding`-Endpoint läuft aber durch — er pollt nur ins Leere und timeout-ed nach 35s mit `verified=false`. Das ist akzeptabel als Übergang, weil keine falsche Information ausgegeben wird, aber als Feature ist die Verifikation derzeit deaktiviert. — Ohne Code-Commit, nur Beobachtung
- [x] **Session 2026-04-26 + 2026-04-27 ins Vault** — Daily 04-26 mit komplettem Done/Narrative/Noch-offen-Block; Daily 04-27 mit Hook-Deaktivierung + Re-Enable-Plan; ZuTun um Punkt 15 erweitert (Forwarding-Verification-Loop-Test als offener Backlog-Eintrag inkl. Re-Enable-Steps). — Ohne Code-Commit, Vault-only

### [[Socibot/Overview|Socibot]] — Sidebar-IA-Refactor + komplette Modul-Doku ins Vault

- [x] **IA-Refactor Sidebar (`dashboard/templates/base.html:487-518`)** — „Neuer Post" + „Neues Video" zu einem Eintrag „Erstellen" zusammengefasst, „Konten" + „Autopilot" raus, „Abo & Zahlung" → „Billing". Neuer `/erstellen/`-Hub (`dashboard/routes/erstellen.py` + `templates/erstellen.html`) mit zwei Cards Post/Video + Letzte-Video-Jobs-Liste aus SQLite. — Ohne Commit, lokal
- [x] **Mediathek mit Sub-Tabs Fotos/Ideen** — `dashboard/routes/media.py:32-58` lädt `automation/sample_content.json`, `templates/media.html` mit Tab-Switcher inkl. URL-Querystring-Persistenz. — Ohne Commit, lokal
- [x] **Calendar-Rewrite zu Monats-Grid + Autopilot-Strip oben** — `dashboard/routes/calendar.py` neu mit `_build_month_grid` + Autopilot-Stats (importiert direkt aus `dashboard/routes/autopilot.py:_bot_running` etc.). `templates/calendar.html` 800 LOC Full-Rewrite — 7-Spalten-Grid, prev/next-Month-Nav, Day-Detail-Pane single-instance unter Grid, alle bisherigen Modals (Add/MultiWeek/Schedule) erhalten. — Ohne Commit, lokal
- [x] **Socibot komplett dokumentiert in `Obsidian/Socibot/modules/` (21 Files, ~3.8k LOC Markdown)** — 14 Modul-Notes (00-Topology bis 14-Findings) + 6 Layer/Cross-Cutting-Notes (15-Layer-Frontend, 16-Layer-Backend, 17-Layer-Database, 18-Routes-Map, 19-Connections, 20-Master-Summary). Jede Aussage Datei:Zeilen-belegt aus der Codebase. 4 parallele Explore-Agenten haben die ~18.6k LOC strukturiert ausgelesen, ich habe die Reports in vault-konformes Markdown destilliert + verlinkt. `Socibot/Overview.md` zeigt jetzt auf den Modul-Index. Memory-Index um Socibot-Projekt-Kontext erweitert. — Ohne Commit, Vault-only

### [[Socibot/Overview|Socibot]] — Findings-Fixes + neuer `socibot-design`-Skill + Skill-Audit über alle Templates

- [x] **12 von 14 Findings gefixt ohne Bot-Break** — H1 (`.env.example` mit 9 fehlenden ENVs: FAL_KEY, MUBERT_API_KEY, OPENAI_API_KEY, Stripe-Trio, Supabase, License/Admin, BASE_URL, CLAUDE_MODEL, VIDEO_MAX_*), L1+L2 (README-Korrekturen `python start_bot.py` + `schedule` statt APScheduler), L4 (TikTok-Stub-Doku im Code), M3 (3 SQLite-Indexes auf `data/video_jobs.db` für status+created/client+created/cost_ledger), M5 (`VIDEO_MAX_WORKERS`/`VIDEO_MAX_RETRIES` über ENV-Override), H2 (`CLAUDE_MODEL` ENV-Override-fähig in `bot/constants.py:76`), L7 (Notif-Cap 200/80 in `notification_service._save`), M1 (Notification-Service silent-catch-Loglein gefixt), H5 (`is_thread_escalated()`-Helper in `dm_handler.py:131-148` + `skip:True` in `handle()` + 5× `result.get("skip"): continue` in `bot/scheduler.py`-Reply-Methoden), M7 (`media.py:_ideas_by_platform` mit Förderkraft-Brand-Filter — andere Marken sehen leeren Ideen-Tab), C4 (Brand-Keywords in `variant_service._brand_keywords_from_knowledge` aus `client/brand_knowledge.json` mit Fallback auf hardcoded-Liste), C3+H4 (`scheduler.setup_schedule` registriert TikTok-Jobs gar nicht erst statt sie pro-Run zu skippen). Bewusst NICHT angefasst: C1 (Auth-Layer — Single-User-Self-Host-Architektur, würde Onboarding-Skip-Flow brechen), C2 (Atomic-Write für ALLE JSONs — 6+ Schreibstellen umstellen ist Risiko), H3 (Persistent Job-State — Refactor zu groß).

- [x] **`socibot-design`-Skill geschrieben** (`~/.claude/skills/socibot-design/SKILL.md`, **1029 LOC**) — code-belegtes Design-System parallel zu `chipy-design` (Phonbots Schwesterskill). 22 Sektionen: Catppuccin-Mocha-Palette + Akzent-Tints (rgba(203,166,247,...)-Skala), Typography-Scale (Page-Title-Cap 20px statt Hero), Sidebar-fixed-Layout, alle Komponenten (Card/Badge/Button/Toast/Modal/Notif), Tab-Switcher / Hub-Cards / Stat-Strip / Drilldown-Pane-Pattern, JS-Helper-Konventionen (escHtml/toast/apiPost/confirmAction), Standalone-Wizard-Pattern, Cat-Mascot-Regeln, A11y-Floor (WCAG 2.1 AA mit konkreten Contrast-Werten), Animations-Budget (max 2 Loops gleichzeitig), Polling-Frequenzen-Tabelle, Z-Index-Skala, 15 Anti-Patterns + Ready-to-paste Page-Skeleton + Workflow-Checklist. Recherche-Quellen: `chipy-design` (~640 LOC, Format-Vorlage), `superpowers-writing-skills` (Description=Trigger nicht Workflow), Socibot-Modul-Notes 00-20 (3.8k LOC), `dashboard/templates/base.html:1-754` (kanonische Source-of-Truth). Im Skills-Index registriert + auffindbar. — Ohne Commit, lokal

- [x] **Skill auf alle 19 Templates angewendet (Pre-Scan + Bulk-Fix)** — Pre-Scan ergab: 6 Templates mit bare `confirm()`/`alert()`, 11 mit bare `fetch()`, 12 mit `innerHTML`-Usage, 14 mit Catppuccin-Hex inline. Fixes: 4× `confirm()` → `confirmAction()` (`calendar.html:712 deletePost`, `lernen.html:177,183` Reset-Funktionen, `postfach.html:262` DSGVO-Delete), 1× `alert()` → `toast()` (`marke.html:350` Validation), Catppuccin-Hex → CSS-Variablen-Bulk-Replace per Python-Script: **`calendar.html`** (komplett von ~26 Inline-Hex auf `var(--green/red/blue/text/surface/accent/...)`), **`lernen.html`** (12 Replacements), **`woche.html`** (2), plus manuelle Fixes in `billing.html` (4× `#f38ba8` → `var(--red)` + Jinja-Conditionals), `erstellen.html` (2× Status-Tints), `media.html` (1× green). Standalone-Wizards (`fragebogen.html`, `setup.html`) bewusst NICHT angefasst — eigene Catppuccin-Inline-Palette ist im Skill explizit erlaubt für „pre-base.html"-Pages. Bare `fetch()` mit FormData / Multipart / Streaming gelassen (`marke.html`-PDF-Upload, `composer.html`-Post-Submit, `media.html`-Upload) — `apiPost` covered nur JSON-Calls. Plattform-Hex (#e1306c IG, #1877f2 FB etc.) sind im Skill §1 explizit erlaubt. — Ohne Commit, lokal

- [x] **Smoke-Test ✅** — Bot restart auf `brrt2mapy`, alle 18 getesteten Routes 200: `/`, `/calendar/`, `/erstellen/`, `/composer/`, `/video/`, `/media/`, `/media/?tab=ideen`, `/analytics/`, `/billing/`, `/lernen/`, `/postfach/`, `/verlauf/`, `/autopilot/`, `/einstellungen/marke`, `/api/status`, `/api/notifications`, `/api/log`, `/landing`. Boot-Log zeigt `[tiktok] Stub-Plattform — Posting-Jobs werden NICHT registriert` (C3-Fix verified). Banner: „Socibot · Claude AI". SQLite-Indexes via `sqlite_master`-Query verifiziert (`idx_video_jobs_status_created`, `idx_video_jobs_client`, `idx_cost_ledger_client`).

## 📝 Narrative

- Round-8-Triggerwort: „mach runde acht und wie gesagt arbeite ganz viel mit codex als zweite meinung und fix as kaputt ist aba achte nicht mehr kaputt zu machen". Stabilität war die explizite Vorgabe — ich habe das erstmals als Codex-Plan-Review-VOR-Implementation interpretiert (statt nur Counter-Review nach Author-Pass). Hat sich gelohnt: die ursprünglichen Regex-Patterns (`act as a`, `you are now a/an`, `system prompt`, `du bist jetzt`) hätten in den False-Positive-Bereich gefallen und legitime Customer-Prompts blockiert. Codex's Code-Review hat das vor Deploy gefangen.
- Cache-Invalidation-Lücke in `/agent-config/new` war ebenfalls Codex-Catch — ich hatte den Pfad übersehen weil er nicht durch `writeConfig()` läuft. Codex's Plan-Q4 hat das schon angedeutet („Falls es Bypass-Pfade gibt, ist die Invalidation per Definition lückenhaft"), bei der Code-Review hat er den konkreten Pfad konkret benannt (`apps/api/src/agent-config.ts:241, :290, :854, :880, :883`). Solche „du sagst es hast es vorhergesagt"-Loops sind exakt der Wert von zwei Reviewer-Augen.
- Per-route-Rate-Limit (60/min) auf Admin-Endpoints ist defensiv: bei einem kompromittierten platform-admin-JWT wäre ein Bulk-Export-Versuch sichtbar (audit-log) UND verlangsamt (rate-limit). Codex hat die Diskussion über fire-and-forget-vs-hard-fail offen gelassen: Best-Effort akzeptabel solange `log.warn` Sentry-sichtbar ist. Bin bei Best-Effort geblieben weil Hard-Fail eine Operations-Falle wäre (DB-Hick blockiert Admin der gerade ein Production-Issue debuggen muss).
- **Hinweis Round-7-Hangover**: Codex hat zur Q5 (DSGVO `learning_corrections.original_text` an OpenAI) gesagt das ist ein Compliance-Gap ohne dokumentierte AV. Round 8 schließt das mit `redactPII()` an der Boundary, aber: die original_text-Rohdaten liegen weiterhin 365 Tage in der DB. Das ist OK — Speicherung mit AV-Vertrag intern ist kompatibel; das eigentliche Problem war die ungefilterten Übermittlung an OpenAI als Auftragsverarbeiter ohne explicit-konfigurierte Daten-Minimierung. Jetzt geht nur noch redacted Text raus.

- Wichtig für die Forwarding-Verification-Re-Enable-Aktion (siehe Daily 2026-04-26): Codex hat **sowohl den Import als auch den Call-Site auskommentiert** (Hint im Code: „with the call site below"). Wenn ich/Vaso die Lokal-Änderungen am `phone.ts` (Helper + Endpoint) committen und pushen, müssen **beide Stellen** wieder aktiviert werden, sonst geht der Hook nicht. Der Type-Check (`tsc --noEmit`) wurde am 2026-04-26 lokal grün gefahren — sollte nach Re-Enable wieder grün sein, aber Build pre-deploy nochmal prüfen.
- Verifier-Trunk: bevor Live-Test überhaupt sinnvoll ist, muss Twilio-DE-Nummer gekauft werden + bewusst NICHT in Retell importieren. Das ist der einzige Hard-Block aktuell.

## 📋 Noch offen

- [ ] **`apps/api/src/phone.ts`-Lokal-Änderungen committen + pushen** (Helper + Endpoint + Migration-Comment-Update + `.env.example`). Dependencies: keine — die Frontend-Änderungen sind unabhängig deploybar.
- [ ] **`apps/api/src/retell-webhooks.ts` Re-Enable**: Import-Zeile (~38-41) auskommentiert wieder aktivieren + Call-Site-Block im `call_started`-Branch (`checkForwardingVerificationMatch(toNumber, fromNumber).catch(...)`). Erst NACH dem `phone.ts`-Push, sonst bricht Prod-Build mit „Missing export `checkForwardingVerificationMatch`".
- [ ] **Frontend-PRs**: `apps/web/src/lib/api.ts` + `apps/web/src/ui/PhoneManager.tsx` + `apps/web/src/ui/agent-builder/CapabilitiesTab.tsx` ebenfalls committen. Können vor oder nach dem API-Push committed werden — Frontend redet nur über REST mit der API, keine Type-Cross-Reference.
- [ ] **Live-Smoke-Test** (siehe [[Daily/2026-04-26]] „Noch offen").

## 🔗 Links

- [[Phonbot/Overview]]
- [[Phonbot/ZuTun]] — Punkt 15 Forwarding-Verification-Loop-Test
- Vortag: [[Daily/2026-04-26]]
