---
date: 2026-04-28
tags:
  - daily
  - 2026/04
mood: focused
---

# 2026-04-28 · Dienstag

> [!tip] Haupt-Fokus
> Vault auf git gehoben (haskallalk-eng/obsidian-vauls, privat, obsidian-git Plugin auto-commit alle 5 min). Recording-Toggle in PrivacyTab End-to-End verdrahtet inkl. persistenter DSGVO-Audit-Tabelle (Round 11). Codex-Verifys vor + nach Deploy haben wieder echte Bugs gefangen — diesmal HIGH (Schema-Default Write-Through) + LOW (Banner-Wording).

## ⚡ Done heute

### [[Phonbot/Overview|Phonbot]] — Vault auf git gehoben

- [x] **`.gitignore` + `git init -b main`** im Vault-Root mit haskallalk-eng-GitHub-Identität (Commit `775fc7c`/`aec8a86` erste 2 Commits, 72 Files initial). `.gitignore` ignoriert `.obsidian/workspace*.json` (UI-State), `.trash/`, OS-junk, Editor-leftovers.
- [x] **GitHub-Repo `haskallalk-eng/obsidian-vauls` (privat) angelegt** + remote auf SSH umgestellt (HTTPS-push scheiterte mangels TTY/Cred-Helper, `~/.ssh/id_ed25519` funktioniert direkt mit GitHub).
- [x] **Sanity-Check vor Push**: 1 Treffer im Secret-Scan war false-positive (`sk_live_ABC123xyz9` als Beispiel-Token im Audit-Doc). Push durch.
- [x] **`obsidian-git`-Plugin (Vinzent03 v2.38.2)** installiert + enabled. In v2.38 heißt der Backup-Command **„Commit-and-sync"** (nicht mehr „Create backup" wie in alten Tutorials). Auto-Backup-Interval `5min`, Auto-Pull `5min`, Pull-on-Startup an. Erste Auto-Backup-Commit (`aec8a86 vault backup: 2026-04-27 18:00:08`) erfolgreich auf Remote angekommen.

### [[Phonbot/Overview|Phonbot]] — Recording-Toggle End-to-End (Round 11)

- [x] **Schema-Feld `recordCalls`** in `agent-config.ts` `AgentConfigSchema`. **Codex pre-deploy code-review fand HIGH-Bug**: `z.boolean().default(true)` würde sich Write-Through-mäßig in Customer-Configs materialisieren — bei jedem `parseAgentConfig`-Round-Trip wäre das Feld plötzlich auf `true` gesetzt, obwohl der User den Toggle nie angefasst hat. Pre-deploy auf `z.boolean().optional()` umgestellt; alle Konsumenten treats `undefined` als legacy-on via `!== false`-Pattern. Frontend-Toggle-Display auf `?? true` korrigiert (matched Backend-Behaviour).
- [x] **Disclosure conditional in `agent-instructions.ts`**: bei `recordCalls !== false` voller Block (KI-Hinweis + Aufzeichnungshinweis + recording.declined-Path), bei `false` nur KI-Hinweis (EU AI Act Art. 50 bleibt Pflicht). Codex review feedback umgesetzt: Wording „Audio + Transkript werden nicht gespeichert" statt „kein Recording" (Retell `basic_attributes_only` behält Metadaten — präziser).
- [x] **`retell.ts:createAgent` + `updateAgent`** bekommen optional `dataStorageSetting`-Param → `body.data_storage_setting` (Retell-API-Field, `opt_out_sensitive_data_storage` ist deprecated). 4 Aufrufstellen in `deployToRetell` (Update + Create + Fresh-Deploy + Callback-Agent) versorgt mit dem Wert basierend auf `recordCalls`. **Live-Verify gegen prod-Retell-API bestätigt**: `data_storage_setting` field name korrekt, GET zeigt aktuelles `everything`.
- [x] **`buildRetellTools`**: `recording_declined`-Tool wird nur registriert wenn `cfg.recordCalls !== false`. Wenn aus → Tool weg (sinnlos, da nichts aufgenommen wird).
- [x] **PrivacyTab.tsx UI-Hint-Block**: Toggle ON → existing yellow DSGVO-Banner; Toggle OFF → grauer Banner mit präzisierten Wording „Audio + Transkript werden nicht gespeichert. Anrufmetadaten (Dauer, Datum, Rufnummer) verbleiben für die Minutenabrechnung." (Codex-Empfehlung: Metadaten-Halbsatz schließt Transparenzlücke). Plus statisch unter beiden: „Änderung greift nach dem nächsten Deploy".
- [x] **Persistente Audit-Tabelle `privacy_setting_changes`** (Codex Round-11 review HIGH): `log.info` über Pino reicht NICHT für DSGVO Art. 5 Abs. 2 / Art. 24 Rechenschaftspflicht — Sentry-retention ist plan-abhängig. Neue Tabelle (org_id, tenant_id, setting, value_before, value_after, changed_by, created_at) mit 365d-Retention via `cleanupOldPrivacySettingChanges` daily cron (110s post-startup, staggered). `agent-config.ts:writeConfig` insertet bei jedem `recordCalls`-Toggle-Wechsel eine Audit-Row best-effort (insert-fail blockiert nicht den save).
- [x] **Type-check API + Web grün, prod-Deploy** (commits `11285e3` + `7aa46e2`). Container Healthy nach 22s. DB-Migration `privacy_setting_changes` in prod via psql verifiziert.

## 📝 Narrative

- Heute war Pattern-Wiederholung aus Round 8-10 mit dem Recording-Toggle-Feature. **3 Codex-Sessions**: (1) Plan-Review zu Pattern-Pool-Fix (industry-Tag fehlt — schließlich verschoben weil User wollte recording-toggle zuerst), (2) Code-Review der Recording-Toggle-Diff vor erstem Deploy, (3) Code-Review der Verify-Round-Diff (Audit-Tabelle + Banner-Wording).
- **Codex hat zweimal vor Deploy gerettet**: einmal mit dem Schema-Default-Write-Through (subtle, hätte ich allein nicht gesehen), einmal mit dem „log.info reicht für DSGVO nicht"-Befund (rechtlich relevant — Sentry-retention wäre nicht belastbar bei einer Behördenprüfung).
- **Vault-auf-git** war ein offener Punkt seit Tagen — User-Frage „sind alle meine vaults auf git" hat klar gemacht das Risk wir tragen mit allen Daily-Notes seit 2026-04-18 ungesichert auf Festplatte. SSH-Key war zum Glück schon da, die ganze Aktion 5 Minuten + Plugin-Setup. Auto-Backup läuft jetzt unsichtbar im Hintergrund.
- **Cross-Review-Pattern macht weiterhin Wert**: Codex Round 11 Q2 (Race im `previousRecordCalls`-Read) als „akzeptabel ohne Lock" verbucht — gut weil ich nicht in panic-mode für theoretische edge-cases gerenne. Codex's Ja/Nein/Mit-Caveat hat klare Pflicht-Action-Sortierung: HIGH (Schema-default) → fixen, MEDIUM (Audit-Log persistent) → fixen, LOW (Race) → akzeptieren, LOW (Banner-Wording) → fixen.
- Pattern-Pool-Fix bleibt in Round 12 — `industry`-Tag fehlt im Schema + Templates, deshalb schießt der ganze Pattern-Pool-Code-Pfad ins Leere obwohl Backend sauber gebaut ist. War heute zu groß weil DSGVO-Privacy-Risiken bei Fallback templateId→industry (Codex hatte schon gesagt: lieber whitelist-basierter Fallback statt naiv).

## 📋 Noch offen

- [ ] **Round 12: Pattern-Pool-industry-Tag** — `AgentConfigSchema` + `Template`-Type um `industry?: string` erweitern, alle existierenden Templates (`hairdresser`, `tradesperson`, etc.) explizit getaggt. Plus Whitelist-Fallback templateId→industry damit cross-org-learning erst loslegt. Codex's Plan-Review (im Hintergrund) muss noch verifiziert werden bevor Implementation.
- [ ] **WEBHOOK_SIGNING_SECRET hard-required** in env.ts (Round 7 hat soft-warn gesetzt, prod-env-var ist seitdem nicht gesetzt worden — sieht im Boot-Log noch immer „WARN: missing recommended production secrets" jedes Mal). User muss erstmal das env-var auf 87.106.111.213 setzen, dann promote-to-hard-required-Commit.
- [ ] **Pattern-Pool-Backfill**: bestehende `call_transcripts.industry` und `call_analyses.industry` sind null. Entweder rückwirkend taggen (basierend auf agent_config) oder akzeptieren dass nur neue Anrufe in den Pool fließen.
- [ ] **`recordCalls`-Tests fehlen** — Codex's „kein Test"-Feedback aus Round 11 noch offen. Beim nächsten Test-Sweep mit-aufnehmen.
- [ ] **Verify-Forwarding live-Smoke-Test** (siehe Daily 2026-04-26 #5 in Phonbot/ZuTun #15) — VERIFIER_TWILIO_NUMBER kaufen + echte Handy-Test-Sequence machen. Bleibt seit 2026-04-26 offen.

## 🔗 Links

- [[Phonbot/Overview]]
- [[Phonbot/ZuTun]]
- [[Phonbot/modules/Backend-Auth-Security]]
- Vorheriger Eintrag: [[Daily/2026-04-27]]
