---
title: 2026-04-30
tags: [daily]
created: 2026-04-30
---

# 2026-04-30

## Sessions

### CLAUDE.md (global) angelegt
Globale `~/.claude/CLAUDE.md` neu erstellt — bisher gab's nur die Auto-Memory. Recherche aus Anthropic Best-Practices, Humanlayer-Blog, awesome-claude-md, awesome-claude-code, UX Planet. Lean (~50 Zeilen), Windows-/Mindrails-spezifisch, ergänzt Auto-Memory ohne zu duplizieren. Inhalt: Identität+Sprache (DE-Default), Win11+Git-Bash-Quirks, Auto-Mode-Regeln, Skill-first, Codex-Rescue, DSGVO-Pflicht, Verify-Gate (Playwright+Chrome-Profil), Doku-Disziplin, Anti-Patterns. Bewusst NICHT drin: Memory-Inhalte, Skills-Katalog, Linter-Aufgaben, Standard-Konventionen.

Nebenbei: Prompt-Injection im Humanlayer-Artikel entdeckt (gefälschter `system-reminder` zu TodoWrite, der versuchte mich gegen den User zu instrumentieren). Ignoriert + transparent gemeldet.

### Phonbot — Prompt-Qualität + Email-Bug-Visibility (Session 2)
Folge-Tasks nach Stimme+Sensitivity. **Course-Correction:** ich hatte "kein Email/SMS wenn man das will" zuerst als Opt-out interpretiert — der User hat klargestellt: emails SOLLEN gesendet werden, klappt aber nicht (Bug). Refactor verworfen, Bug-Visibility-Fix gemacht.

**Geändert:**
- `platform-baseline.ts` (universal): Zahlen-Diktion (ziffer-für-ziffer mit Pausen), Email-Provider-Whitelist (~30 DACH-Provider, Custom-Domains buchstabieren-Pflicht), DIN-5009-Modus-Trigger explizit
- `demo.ts` DEMO_END_INSTRUCTIONS: Selbstreflexion-Sektion (Chipy-Identität, Meta-Fragen-Handling, max 1-2 Meta-Antworten dann zurück zur Demo)
- `email.ts`: `send()` und `sendSignupLinkEmail()` returnen `{ok, error}` statt void → Fehler werden nicht mehr verschluckt
- `demo.ts` /demo/callback: beide signup-link-email-Aufrufe loggen Erfolg/Fehler via Pino mit kind/branch/leadId
- Cache-Keys v5→v6 (demo + sales) wegen Baseline-Wachstum

**Email-Bug Root-Cause unbekannt** — Code-Visibility ist drin. User muss prüfen:
1. Resend-Dashboard auf Bounces/Domain-Verification/Rate-Limit
2. Pino-Logs auf Prod nach Deploy auf `kind: 'signup_link'` filtern
3. RESEND_API_KEY + EMAIL_FROM in apps/api/.env

**Admin-Override-Caveat:** PLATFORM_BASELINE / DEMO_END_INSTRUCTIONS / DEFAULT_SALES_PROMPT haben DB-Overrides via `demo_prompt_overrides` (rows `__platform__`/`__global__`/`__sales__`). Falls dort ein Override liegt → Code-Default unsichtbar bis "Restore Default" in Admin-UI oder PUT mit null/null. Endpoint: `/admin/demo-prompts`.

### [[Socibot/Overview|Socibot]] — W0.5 Eval-Framework Track 1 (eval_logger)

- [x] **eval_logger.py implementiert** (~230 LOC) — `log_generation`, `record_user_feedback`, `get_recent_entries`, `rotate_old_logs`, `compute_eur_cost`; HMAC-Pseudonymisierung, Fernet-Encryption at-rest, FileLock Concurrency, JSONL-Append
- [x] **eval_retention.py** — Cron-Job-Wrapper analog `compliance_retention.py`; liest `EVAL_LOG_RETENTION_DAYS` aus ENV
- [x] **tests/test_eval_logger.py** — 34 Tests, alle grün; Fixtures mit `monkeypatch` + `tmp_path`, kein echter .env nötig
- [x] **.env.example** ergänzt — `EVAL_LOG_RETENTION_DAYS=90` unter DSGVO-Block
- [x] **py_compile clean** auf allen drei neuen Dateien

### Phonbot — Demo-Stimme + Empfindlichkeit (Session 1)
Drei Themen geklärt → siehe [[Phonbot/ZuTun]].
1. **Warum nur eine HQ-DE-Stimme?** Designregel im Voice-Catalog: HQ-Tier nur bei echt-nativen Recordings. ElevenLabs `11labs-*` sind alles englischsprachige Schauspieler → auf DE = Akzent → bewusst aus DE-Lineup ausgeschlossen. Native HQ DE = nur Hassieb-Kalla-Custom-Clone. Mehr HQ-Optionen = mehr deutsche Sprecher klonen (+€0.05/min pro Klon).
2. **Demo-Stimme:** Code nutzt bereits `DEFAULT_VOICE_ID` = Hassieb-Kalla. Aber: Demo-Agents sind in Redis 24h gecacht — pre-2026-04-18-Cache-Hits laufen noch mit alter Cartesia-Stimme. Cache-Key v4→v5 gebumpt → erzwingt Recreation.
3. **Empfindlichkeit:** Demo + Sales-Callback liefen auf `interruption_sensitivity=1.0` (Max). Hardcoded auf `0.5` runtergedrückt — nur Demo+Sales, paying-tenants behalten ihre eigene Tuning-Konfig.

Status: Code geändert + typecheckt grün. **Nicht committed/gepusht/deployed.** User muss noch:
1. `git commit` + `git push origin master`
2. `ssh root@87.106.111.213 'cd /opt/phonbot && bash scripts/deploy.sh'`

Files:
- [[Phonbot/ZuTun]]
- `~/.openclaw/workspace/voice-agent-saas/apps/api/src/demo.ts`

### [[Socibot/Overview|Socibot]] — W0.5 Eval-Framework Track 2: Cost-Tracker

**Kontext:** W0.5 ist ein paralleles 4-Track-Framework fuer LLM-Qualitaets- und Kostenmessung in Socibot. Track 1 baut `eval_logger.py` (Logging-Foundation), Track 2 (diese Session) den Cost-Tracker, Track 3 `llm_judge.py`, Track 4 Gold-Set + Baseline-Tools.

**Neue Files (keine bestehenden Files veraendert):**
- [x] **`dashboard/services/cost_tracker.py`** (~260 LOC) — aggregiert EUR-Kosten aus `eval_logger.get_recent_entries()` (lazy import, Track 1 parallel), vergleicht mit Tier-Sollwerten, berechnet Drift-Severity (`ok`/`warn`/`alert`), persistiert in SQLite WAL-Mode unter `data/eval/cost_history.db`. — Ohne Commit
- [x] **`dashboard/routes/eval_dashboard.py`** — Flask Blueprint `/eval/`, Operator-Gate identisch zu `brand_settings._is_operator_request()`. GET `/eval/` + GET `/eval/api/snapshot`. — Ohne Commit
- [x] **`dashboard/templates/eval_dashboard.html`** — Catppuccin-Mocha, extends `base.html`, 4 Tier-Cards, Alert-Banner. Vanilla JS. — Ohne Commit
- [x] **`tests/test_cost_tracker.py`** — 24/24 Tests gruen. Deckt: `_compute_eur_cost` Edge-Case (unbekanntes Modell), `_calc_drift` (ok/warn/alert/negativ), Snapshot-Aggregation, SQLite-Roundtrip, WAL-Mode, `daily_snapshot_job`.

**Design-Entscheid:** `_calc_drift` prueft `abs(drift)` — negative Drift (Kosten weit UNTER Soll) triggert ebenfalls Alert, da das auf fehlende Logs hindeutet.

**Offene Risiken:** SQLite-Lock bei >4 Gunicorn-Workers; Scheduler-Registrierung fuer `daily_snapshot_job` ausstehend; eval_logger (Track 1) noch nicht verfuegbar (Fallback: leere Liste).

Siehe [[Socibot/ZuTun]].

### [[Socibot/Overview|Socibot]] — Welle 0 (Phase 1.1 Close) — 9 Commits

Nach W0.5-Track-4 Pause. 66 uncommitted Files (nach Wochen Phase-1.1-Arbeit) wurden in **9 thematische Commits** gebündelt (kein Push). Cluster-Plan vom Explore-Agent, ausgeführt von Sonnet-Agent unter strict-no-push-Mandat.

| Hash | Cluster |
|---|---|
| `f1c3d72` | feat(auth): user registration, login, account deletion |
| `1b30163` | feat(meta-oauth): Click-to-Connect für Instagram + Facebook |
| `611691d` | feat(ui): Erstellen-Hub für Composer + Video-Generator |
| `d56f53b` | feat(dsgvo): Datenschutz, Impressum, Meta-Setup-Doku |
| `6755f38` | feat(ui): Sidebar-Refactor + Calendar/Marke/Postfach Updates |
| `e3572f0` | feat(routes): refactor dashboard routes für Auth + Connections |
| `66849c3` | feat(services): notification, plan, video-engine updates |
| `f8666c8` | chore: dependencies, .env.example, README, .gitignore |
| `b6bbca1` | feat(compliance): Risk-Check v2 + W0.6 Stabilisierung |

**Bonus-Issue beim Verifikations-Pass**: Github-PAT (`ghp_...`) im Klartext in `.git/config` Remote-URL erkannt. Token gehört auf `Hansweier`-Account, könnte der suspendierte sein. Nicht von dieser Session verursacht. Empfehlung: `git remote set-url origin https://github.com/Hansweier/social-media-saas.git` + PAT rotieren.

### [[Socibot/Overview|Socibot]] — W0.6 Compliance v2 Stabilisierung

Risk-Check-v2-Code wurde von einem Sonnet-Agent **ungeplant gebaut** statt nur designed → Reviews nötig.

**Opus-Code-Review** fand 5 P0-Blocker:
- B1 fehlende File-Locks auf `compliance_audit.jsonl` + `compliance_cache.json`
- B2 relative Pfade (`Path(".")` statt `Path(__file__).resolve().parent.parent.parent`)
- B3 `UWG5-02` False-Positive (blockt jede legitime „kostenlose Erstberatung", BVerfG-konform)
- B4 `HEILPRG-02` verfehlt das eigene example aus dem JSON (Reihenfolge-Bug)
- B5 Trial-User mit Persona „arzt" verbrennt Owner-API-Key (kein Cost-Cap, Persona kommt aus Request statt brand_knowledge)

**security-dsgvo-Review** fand 3 CRITICAL:
- C-01: `data/compliance_audit.jsonl` + `compliance_cache.json` nicht in `.gitignore` → Token-/PII-Leak
- C-02: alle `/compliance/*`-Routes ohne Auth, `/audit` zeigt allen Usern alle Override-Events, `/prune-audit` ohne Operator-Gate
- C-03: Post-Content im Klartext an Anthropic ohne AVV/SCCs/Opt-Out → §203-StGB-Risiko für Anwalt/Arzt

**4 parallele Sonnet-Tracks fixed:**
- Track A: File-Locks + absolute Paths + HIGH-01 Cache-Hash-Only + HIGH-05 Layer-2 fail-closed für arzt/heilpraktiker + B5 `get_persona_for_user()` aus brand_knowledge + Cost-Cap pro User/Monat
- Track B: Auth-Helper `_require_auth()` + `_require_operator()`, alle 6 `/compliance/*`-Routes geschützt, `/audit` user-scoped, `/prune-audit` mit ADMIN_SECRET, `pattern_id`+`matched_text` aus API-Response gestrippt
- Track C: Pipeline-Integration in `variant_service.generate_variants()` — Compliance-Floor nach jeder Variant, regen-once bei Hard-Block
- Track D: Cron-Job `bot/jobs/compliance_retention.py` täglich 04:30, scheduler.py Hook

**Opus-Integration-Review** fand 3 weitere Must-Fixes:
- `routes/compliance.py:23` AUDIT_FILE relativ statt absolut → `from compliance_service import AUDIT_FILE`
- `compliance_retention.py` ignoriert `COMPLIANCE_AUDIT_RETENTION_DAYS`-env → durchreichen
- Cost-Estimate hardcoded €0.001 (15× zu niedrig) → Konstante `LAYER2_COST_PER_CALL_EUR=0.014` aus env

C-03 (Layer-2-Opt-Out + AVV + Onboarding-Disclosure) bewusst offen für Pre-Live-Customer.

Verdikt: **Persona-A-Production: WAS NO-GO**, jetzt **CONDITIONAL GO** (mit C-03).

→ Commit `b6bbca1` (in Welle-0-Cluster gemerged).

### [[Socibot/Overview|Socibot]] — W0.75 API-Key-Frei-Sweep

Codex-vorgezogen vor W0.5: Customer-DoD-Hard-Stop. UI hat KEINE Token-Forms mehr.

**2 parallele Sonnet-Tracks:**
- UI: `marke.html` Claude-Block raus, 3 Coming-Soon-Cards für LI/X/TT (Catppuccin-Mocha mauve-Border + gelbe Badges), SMTP in collapsed `<details>` mit Operator-Hinweis. `setup.html` Step 1 (Claude) entfernt, Wizard 5→4 Steps, `PLATFORM_CONFIGS` für LI/X/TT auf `oauth: true`. `overview.html` Wording aktualisiert. Dead `testToken/testPlatform` JS-Funktionen entfernt.
- Backend: `brand_settings.py` `_test_claude/instagram/facebook` raus, `_test_linkedin/twitter` mit DEPRECATED-Marker, neue `_is_operator_request()`-Helper (env-Flag ODER ADMIN_SECRET-Header/Body), `_build_token_values()` reduziert auf SMTP, `_build_token_status()` IG/FB OAuth-only

**Opus-Final-Review fand 5 P0** (alle gefixt):
- `/einstellungen/token-speichern` + `/token-testen` aus `PUBLIC_PREFIXES` entfernt
- `apiPost()` in base.html mit `X-CSRFToken`-Header + `credentials:'same-origin'`
- `token_status[linkedin/twitter/tiktok]` hart auf `False` (sonst zeigt UI „Aktiv" wenn .env-Token gesetzt)
- `token_status['claude']` aus Customer-Dict entfernt (Operator-zentral, gehört nicht ins UI)

→ Commit `e0b35f3`.

### [[Socibot/Overview|Socibot]] — W0.5 Eval-Framework: Track 3 (llm_judge) + Integration

Dritter und letzter Track des W0.5-Frameworks (vor Integration). LLM-Judge: Generator=Sonnet, Judge=Opus → kein Same-Model-Bias. 5-Dimensionen-Rubric in `data/critique_rubric.json` (Brand-Voice 30%, Audience-Fit 20%, Specificity 20%, Engagement-Hook 15%, Platform-Fit 15%). 30/30 Tests grün.

**Integration-Step**:
- `variant_service._generate_with_instruction()` returns jetzt `tuple (content, meta)` mit `prompt`, `tokens_in/out`, `latency_ms`, `model_used`
- `variant_service.generate_variants()` ruft `eval_logger.log_generation()` nach jeder Variant (fail-open)
- Variant-Dict bekommt `log_entry_id` für späteres `record_user_feedback()`
- Blueprint `eval_dashboard_bp` registriert in `dashboard/__init__.py`
- Scheduler-Hooks: `_run_eval_log_rotation` (täglich 04:35) + `_run_eval_cost_snapshot` (täglich 04:40)

**164 Tests grün, 1 skipped** (test_arzt_implicit_heilversprechen_soft_block — W1.5-pending).

→ Commit `c60e7bb`.

### [[Socibot/Overview|Socibot]] — Codex-Final-Review der 3 Wellen W0.6 + W0.75 + W0.5

Architektur-Sanity vor W2 (Tier-Backend). Codex-Verdikt: **„Foundation strukturell in Ordnung, aber W2 ist noch kein dünner Aufbau darauf — Tier-Modell, Baseline und Telemetrie haben echte Inkonsistenzen."**

**5 P0-Findings:**
- P0 Tier-Vokabular-Mismatch: `cost_tracker.py` hat nur `trial/starter/pro/agency`, `premium/maximum` existieren nirgendwo
- P0 Baseline-Pfad nicht vertrauenswürdig: `tools/eval/run_baseline.py` ruft `log_generation()` mit alter Mock-Signatur auf (`output_text/score/label` statt `output_full/tokens_in/...`)
- P1 Compliance-Floor instabil (B3+B4 Pattern-Bugs)
- P1 Eval-Telemetrie nicht an User-Outcomes gekoppelt: `record_user_feedback()` wird nirgends in `approval.py` aufgerufen
- P1 C-03 nicht end-to-end: `skip_layer2`-Param existiert nicht im aktuellen Code

**3 Stops vor W2:**
1. Baseline-Runner reparieren + 250-Case-Run
2. W1.5 Pattern-Refresh (UWG5-02 + HEILPRG-02)
3. `premium/maximum` als first-class quality_tier orthogonal zum plan-tier

### [[Socibot/Overview|Socibot]] — W0.5b Codex-Quick-Fixes

Stop 1 abgearbeitet:
- `tools/eval/run_baseline.py` log_generation-Call mit vollständiger Signatur (alle 11 Params), echte Latency-Messung via `time.monotonic()`
- `dashboard/routes/approval.py` `record_user_feedback()`-Hook in `approve_post()` und `reject_post()` (fail-open) — Eval-Loop ist jetzt geschlossen

→ Commit `da07009`.

### [[Socibot/Overview|Socibot]] — W1.5 Pattern-Refresh

Stop 2 abgearbeitet (Pattern-Bugs aus B3/B4):
- `UWG5-02` (Anwalt): Pattern jetzt nur Trigger wenn kostenlos+Erstberatung+Garantieversprechen kombiniert. Verifiziert: legitime „kostenlose Erstberatung" → PASS, „kostenlose Erstberatung mit Erfolgsgarantie" → BLOCK, „kostenlose Beratung garantiert" → BLOCK
- `HEILPRG-02` (Heilpraktiker): bidirektionales Pattern (X→Arzt UND Arzt→X). „Statt zum Arzt: kommen Sie zu mir für eine Diagnose" → BLOCK
- `HEILPRG-03` NEU: Implicit-Heilversprechen für Schmerz/Migräne/Asthma/Burnout/Depression/Tinnitus/Krebs als soft_block
- `test_arzt_implicit_heilversprechen_soft_block` UNSKIPPED, persona auf `heilpraktiker` umgestellt

**165 Tests grün, 0 skipped.** Bekannte Lücke: „garantieren VOR kostenlos" wird nur via Layer-2 Claude-Pass gefangen (semantisch zu komplex für Regex). Realistisches Pattern-Audit mit echtem Anwalt + Heilpraktiker als Reviewer bleibt User-Aufgabe.

→ Commit `3a6562b`.

### [[Socibot/Overview|Socibot]] — C-03 DSGVO-Implementierung (Layer-2-Opt-Out + AVV + Onboarding)

- [x] **`compliance_service.py` — skip_layer2-Param implementiert** — `check_compliance_v2()` erhält `skip_layer2: bool = False`; `run_layer2_flag` prüft jetzt `not skip_layer2` als ersten Guard; `layer2_skipped_by_opt_out`-Flag berechnet ob Opt-Out aktiv war UND Layer-2 sonst gelaufen wäre; Audit-Log erhält `layer2_skipped`-Field (immer geloggt wenn opt-out aktiv, auch bei passed-Posts — DSGVO-Traceability); WARNING-Log bei HIGH_RISK_PERSONA + skip_layer2=True. — Ohne Commit
- [x] **`dashboard/routes/compliance.py` — skip_layer2 forwarding** — `/compliance/check`-Route liest `skip_layer2` aus Body und reicht weiter; Docstring aktualisiert. — Ohne Commit
- [x] **`dashboard/routes/quality_settings.py` — DSGVO-Opt-Out-Setting + neue Route** — `_get_skip_layer2()` / `_set_skip_layer2()` Helper (atomic write in `client/quality_settings.json`, FileLock); neue Route `POST /einstellungen/qualitaet/skip-layer2`; `qualitaet_page()` reicht `user_skip_layer2` ans Template. — Ohne Commit
- [x] **`dashboard/templates/qualitaet.html` — UI-Toggle** — Warning-Box mit Checkbox „KI-Tiefen-Compliance-Prüfung deaktivieren"; §203-StGB-Hinweis + SCCs-Hinweis; `setSkipLayer2()`-JS mit Fehler-Rollback; Status-Feedback-Text. — Ohne Commit
- [x] **`dashboard/services/user_service.py` — `update_fields()`** — neuer Helper für partielles User-Update; schützt `id`/`email`/`password_hash` vor Überschreiben. — Ohne Commit
- [x] **`dashboard/routes/auth.py` — Pflicht-Checkboxen** — `anthropic_consent` + `schweigepflicht_consent` als required im POST-Handler; Timestamps `anthropic_consent_at` / `schweigepflicht_consent_at` per `update_fields()` persistiert. — Ohne Commit
- [x] **`dashboard/templates/auth_register.html` — Disclosure-Checkboxen** — zwei neue `required`-Checkboxen mit DSGVO-Text (Anthropic-Drittlandtransfer + §203-Schweigepflicht-Eigenverantwortung). — Ohne Commit
- [x] **`public/datenschutz.html` — Anthropic-Sub-AVV-Abschnitt** — vollständiger Sub-Auftragsverarbeiter-Abschnitt (Anthropic Inc., AVV-URL, SCCs 2021/914/EU, Anthropic-Garantien, Opt-Out-Hinweis); TOC aktualisiert. — Ohne Commit
- [x] **`docs/DSGVO_NOTES.md` — AVV-Dokumentation** — neuer Abschnitt mit allen relevanten Feldern (Anbieter, Dienst, Zweck 1-4, AVV-URL, SCCs, Datenfluss, Retention, Customer-Opt-Out, Onboarding-Disclosure, Audit-Log-Flag). — Ohne Commit
- [x] **Tests** — 6 neue `TestSkipLayer2OptOut`-Tests in `tests/test_compliance_v2.py`; 52/52 Tests grün, keine Regressionen. — Ohne Commit

### Bilanz Tag

| | |
|---|---|
| Commits | **19** (lokal, kein Push) |
| Tests | **165 grün, 0 skipped** |
| Code-Δ | +18436/-1362 lines, 103 Files |
| Memory-Note | 15 Tage alt → wird jetzt aktualisiert |
| Push | offen — User reviewt 19 commits, dann decide |

Offen:
- W1.6: `quality_tier` orthogonal zum plan_tier (~1-2h)
- C-03: Layer-2-Opt-Out + AVV + Onboarding (Pre-Live-Customer, ~2-3h)
- W2: Tier-Router + Hook+Body-Split + Self-Critique (4-6h)
- B3/B4-Folge: Anwalt+Heilpraktiker Pattern-Audit (User-Aufgabe)

### [[Socibot/Overview|Socibot]] — W0.5 Eval-Framework Track 4: Gold-Set + Baseline-Tools

**Kontext:** Vierter und letzter paralleler Track des W0.5-Eval-Frameworks. Baut die Eval-Pipeline-CLI: Gold-Set-Generator, Baseline-Runner, Compare-Tool mit CI-Block.

**Neue Files (keine bestehenden Files veraendert):**
- [x] **`tools/__init__.py`** + **`tools/eval/__init__.py`** — Package-Stubs
- [x] **`tools/eval/generate_gold_set.py`** — CLI `--limit N --dry-run`; `generate_all_test_cases()` (Lazy-Generator, 250 Cases = 5P×10T×5Plat), `append_to_gold_set()` (Append-only, SHA-256[:8]-Deduplizierung). `data/eval/gold_set.jsonl` als Default-Target.
- [x] **`tools/eval/run_baseline.py`** — CLI `--label --limit --persona --platform`; `load_gold_set()` (filter+limit), `run_baseline()` (generiert direkt via Anthropic-Client, lazy-importiert llm_judge+eval_logger, aggregiert per Persona/Platform/Dimension, speichert BaselineSnapshot-JSON unter `data/eval/baselines/`).
- [x] **`tools/eval/compare_baseline.py`** — CLI `BEFORE.json AFTER.json --block-threshold`; `compare_baselines()` (per-Persona/Platform/Dimension-Delta, Block-Flag wenn Persona > threshold gefallen), exit(1) bei Block — CI-Hook-ready.
- [x] **`tests/test_gold_set.py`** — 15 Tests, alle gruen: Case-Count 250, keine Duplikat-IDs, 10 Topics/Persona, Append-Deduplizierung, Schema-Felder, Difficulty-Values, item_id-Determinismus.
- [x] **`tests/test_baseline.py`** — 16 Tests, alle gruen: compare_baselines no-block/block/partial-block, Dimension-Deltas, load_gold_set-Filter-+Limit, FileNotFoundError.
- [x] **31/31 Tests gruen**, **py_compile clean** auf allen Files.

**Smoke-Tests bestaetigt:**
- `generate_gold_set.py --dry-run --limit 5` gibt korrekte Items aus
- `compare_baseline.py BEFORE AFTER` -> exit 0 (Verbesserung), exit 1 (Regression mit `<-- BLOCK`)

**Offene Risiken:** `run_baseline.py` benoetigt echte `llm_judge` + `eval_logger` Imports (Tracks 1+3) — in Tests via Mocks abgedeckt, Real-Run erst nach Track-Integration moeglich. Kosten-Schaetzung in `run_baseline.py` ist approximativ (Sonnet-4-6 / Opus Preise 2026-04, kein live Preisfeed).

### Phonbot — Audit Round 14 (Plan-Review → Code → Codex-Review → Fix → Verify)

Zwei Codex-Rounds, beide synchron via `codex:rescue`. Pattern: Plan vorher, Review danach (User-Vorgabe).

**Plan-Review (Codex):** verworfen Variante B (LLM-Auto-Tag — Pool-Poisoning-Risiko), bestätigt Variante A (per-Tenant admin trigger), forderte (b) Tests vor (a) Backfill, fand 4 zusätzliche Mediums (M1 changed_by=orgId, M2 RETELL_TOOL_AUTH_SECRET, M3 Stripe-customer-id-mapping, M4 historische call_transcripts.industry).

**Implementiert:**
- Backend [insights.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/insights.ts): `POST /admin/agents/backfill-industry` mit `{ tenantId, industry, dryRun }`-Body. Validierung gegen `CURATED_INDUSTRY_KEYS`. Update via `jsonb_set(data, '{industry}', ...)` + historische `call_transcripts.industry` (M4-Fix). 404/409/dryRun-Branches. Response `{ processed, transcriptsUpdated, remaining }` wie embed-backfill.
- M1-Fix: `writeConfig(config, orgId?, actorUserId?)` — neuer Param. PUT/POST-Routes reichen `userId` aus JWT durch. `privacy_setting_changes.changed_by = actorUserId ?? orgId ?? null`.
- Refactor: `deriveWebhookSecret(tenantId, webhookId): Buffer` als named export aus [inbound-webhooks.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/inbound-webhooks.ts) — alte unused hex-returning Variante gelöscht.

**Tests (13 cases, alle grün):**
- `webhook-secret-derivation.test.ts` (6): WEBHOOK_SIGNING_SECRET > JWT_SECRET, JWT-Rotation lässt per-hook-Key unverändert, distinct (tenant, hook) → distinct secret, 32-byte digest, Determinismus.
- `agent-instructions-recording.test.ts` (4): recordCalls true/false/undefined → Aufzeichnungshinweis-Block on/off/on (legacy).
- `template-learning-industry.test.ts` (3): industry=null → early-return, industry-set → peer-orgs-Query mit $1, share_patterns=false → short-circuit.

**Codex Code-Review #1 fand 2 HIGH (must-fix vor Ship):**
- A1 — Race-Condition: zwei concurrent admin-calls könnten widersprüchlich-state schreiben. Fix: `pg_advisory_xact_lock(sha1(tenantId).bigint)` + ganze SELECT-write-write in `pool.connect()` + BEGIN/COMMIT/ROLLBACK in try/finally + `client.release()`.
- A4 — Auth zu schwach: `payload.admin` allein liesse manipuliertes User-JWT durch. Fix: zusätzlich `payload.aud === 'phonbot:admin'` (identisch zu admin.ts:55).
- Plus A6 (kein rate-limit): `config: { rateLimit: { max: 30, timeWindow: '1 minute' } }`.
- Plus D2 (test env-leak): `afterAll(() => vi.unstubAllEnvs())`.

**Codex Verify-Pass (#2):** „verify-pass clean, ship". `pnpm typecheck` clean, 13/13 R14-Tests grün.

**Commit:** `01858ca feat(audit-round-14): industry-backfill admin route + recording/webhook tests + M1 actor` (6 files, +450/-38). Push zu master noch ausstehend (auto-mode-Block).

**Deferred zu R15:**
- M2 `RETELL_TOOL_AUTH_SECRET` als hard-required (vor JWT-Rotation Pflicht)
- M3 Stripe `subscription.deleted/pause/resume`-Branches: stripe_customer_id-Mapping statt sub.metadata.orgId
- A3/E1/E2/E3: Integration-Test für Backfill-Route, Concurrency-Regression-Test, batched UPDATE bei call_transcripts (für sehr grosse Tenants)
- (c) Verify-Forwarding live-smoke (Hardware-only) und (d) JWT_SECRET-Rotation (operativ, User-Go).

Beteiligte Notes: [[Phonbot/ZuTun]].

### Phonbot — Audit Round 15 (M2 + M3 aus R14-Backlog)

User hat Plan-Review übersprungen ("ok weiter") — direkt zur Implementation. Codex-Code-Review danach lief, Account war kurz im Cooldown, beim Retry voller Befund.

**M2 — RETELL_TOOL_AUTH_SECRET soft-warn**: nach REQUIRED_PROD_SECRETS in [env.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/env.ts) ein stderr-WARNING ergänzt. Kein hard-required: existing Retell-Agents haben ihre tool_sig mit JWT_SECRET signiert, hard-required ohne migration-period würde alle brechen. Migration-Strategie identisch zu R7→R13 für WEBHOOK_SIGNING_SECRET (set RETELL_TOOL_AUTH_SECRET = JWT_SECRET in prod, dann promote). [.env.example](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/.env.example) bekommt eigenen Block mit voller Migration-Doku.

**M3 — Stripe customer_id resolver**: neue exported `resolveOrgIdFromSubscription(sub)` in [billing.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/billing.ts) — gleiche defence-in-depth wie syncSubscription:402-418, returnt null wenn weder DB noch metadata greifen. Vier Stripe-event-branches umgestellt (deleted/paused/resumed/created), jeder mit log.warn+skip wenn null statt falsche-org-mutation. syncSubscription bleibt mit eigener Inline-Form (subtle anders: mutiert orgId im Verlauf).

**Tests (8 cases):** [billing-resolve-orgid.test.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/__tests__/billing-resolve-orgid.test.ts) — DB-vs-metadata precedence (match + mismatch), metadata-fallback bei DB-miss, null wenn beidem leer, no-DB-call wenn customer fehlt, expanded-customer-object, DeletedCustomer-shape. Inkl. Assert auf den warn-side-effect bei Mismatch.

**Codex Code-Review:** 1 HIGH + 2 LOWs. HIGH war stale Mock-Export-Names (`sendUsageWarningEmail`/`sendInvoicePaymentFailedEmail` statt `sendPlanActivatedEmail`/`sendPaymentFailedEmail`) — Tests waren grün, aber latent broken bei künftigen Webhook-Branch-Tests. Plus LOWs: warn-side-effect-assertion + DeletedCustomer-Case. Alle drei vor Commit gefixt.

**Verifikation:** pnpm typecheck clean, 21/21 audit-tests grün (R14: 13 + R15: 8). 2 pre-existing auth-flow.test.ts-Failures sind nicht von R15.

**Commit:** `494c0d5 feat(audit-round-15): RETELL_TOOL_AUTH_SECRET soft-warn + Stripe customer_id resolver` auf branch `round-14-industry-backfill`. Beide R14-+R15-Commits hängen an demselben Feature-Branch — wenn der PR gemerged wird, ist beides drin.

**Deferred zu R16:**
- RETELL_TOOL_AUTH_SECRET promotion zu REQUIRED_PROD_SECRETS (sobald prod env migriert ist)
- subscription pause/resume Integration-Tests via Fastify-inject + raw stripe-event-fixture
- Last-resort `WHERE stripe_subscription_id = $1` Fallback wenn metadata + customer-mapping beide leer (Codex MEDIUM B4)
- A3 batched UPDATE bei call_transcripts (für sehr grosse Tenants)
- E1/E2/E3 Integration-Tests für Backfill-Route + privacy_setting_changes-Persistenz

### Phonbot — Audit Round 16 (Codex Plan-Review → Code → Self-Review da Codex im Cooldown)

Codex Plan-Review hat empfohlen: (3) → (2) → (4) → (E1+E3) zusammen, (E2) nach R17. Reihenfolge mit Codex-Plan exakt befolgt.

**Implementiert:**
- **(3) 3-tier resolver** in [billing.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/billing.ts): customer → metadata → subscription_id. Last-resort fängt Import-Orphan-Edge-Cases.
- **(4) Phase-1/Phase-2-Split** in [insights.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/insights.ts) (Codex HIGH C: lock muss vor batched UPDATE released werden). Phase 1 commits config in tx, Phase 2 batches transcripts in 1000er-Loops außerhalb des Locks. `client.release()` exakt einmal via try/finally.
- **(2) Stripe webhook Integration-Tests** (Codex HIGH D: kein constructEvent-Mock — echter signature-roundtrip mit `Stripe.webhooks.generateTestHeaderString`). 5 cases. Mock-state-leak via `mockReset()` statt `clearAllMocks()` gefixt.
- **(E1+E3) Backfill-Route Tests** (10 cases): auth/aud-Gating, curated-key, 404/409, dryRun, happy-path, advisory-lock-key-derivation pin, concurrency-409-simulation.

**Verifikation:** pnpm typecheck clean. 134/136 (38/38 audit-tests R14:13 + R15:8 + R16:17, 2 pre-existing auth-flow-Failures unrelated).

**Codex Code-Review:** Cooldown bis 20:50, Self-Review parallel. Selbst-Finding: Partial Index für Phase-2-Performance bei riesigen Tenants → R17.

**Commit:** `967b1c7 feat(audit-round-16): backfill batched + 3-tier resolver + integration tests` auf `round-14-industry-backfill`. Stack jetzt: 3 Commits (R14+R15+R16), 13 Files, +1490/-90.

**R17-Backlog:** Partial Index für call_transcripts, separater backfill-transcripts Endpoint, E2-Test, 2026-04-26-Audit-Ledger-offene (auth/billing/calendar/tickets HIGH+MEDIUMs), deleted-event minutes_cap-Test, RETELL_TOOL_AUTH_SECRET-Promotion, Codex-Code-Review-Retry für R16.
