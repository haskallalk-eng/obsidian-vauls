---
title: 2026-05-02
tags: [daily]
created: 2026-05-02
---

# 2026-05-02

## Sessions

### Phonbot — Audit Round 18 (Backfill-Recovery + Downgrade-Cap + Tests)

Pattern wieder strict: Codex Plan-Review zuerst → Code → Codex Code-Review → 2 MEDIUM-Fixes → Commit. R18-Items aus dem R17-Backlog.

**Implementiert (3 Items + 10 Tests):**

1. **`POST /admin/agents/backfill-transcripts`** in [insights.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/insights.ts) — Companion zu /admin/agents/backfill-industry für Phase-2-Crash-Recovery. R16 known-limitation: Phase 1 commits config-row, Phase 2 batches transcripts außerhalb des Locks; ein Crash dazwischen tagged config aber lässt transcripts NULL — re-run der original Route hits 409 ALREADY_TAGGED gate. Neuer Endpoint liest die getagged industry aus dem org's config(s) und resumed nur den transcript-batch-loop. Body `{ orgId, industry?, dryRun? }`. industry derived via `SELECT DISTINCT data->>'industry'` wenn omitted, 409 AMBIGUOUS_INDUSTRY wenn org-configs disagree (Codex Plan-Review B).

2. **billing MEDIUM-1 mid-cycle Plan-Downgrade `minutes_used` cap** in [billing.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/billing.ts) syncSubscription. Pre-UPDATE SELECT widened um minutes_limit zu lesen (Codex C: kein zweiter round-trip). Wenn `newLimit < oldLimit` UND nicht resetMinutes → composition `minutes_used = LEAST(minutes_used, $7::int)` inline. Atomic, kein read-modify-write race.

3. **subscription.updated Tests** (3 cases) plus 6 Tests für /admin/agents/backfill-transcripts.

**Codex Code-Review fand 2 MEDIUMs:**
- **B2**: silent free-fallback bei unknown stripe price.id wurde durch R18 destruktiv (würde minutes_used auf 30 cappen wenn ops eine neue Stripe-Price-ID vergisst). Fix: log.warn + suppress LEAST-clause wenn matchedPlan null. Plus Test der das pinnt.
- **C2**: Test-Fixture nutzte fragile unknown-price→free fallback statt echte Starter-PRICE-ID. Fix: STRIPE_PRICE_STARTER env-stub + neuer `priceId`-Param in makeSubscriptionEvent. Plus bind-check assertions ($2='starter', $7=360).

**Verifikation:** typecheck clean, 151/153 (2 pre-existing auth-flow). Audit-Test-Count: R14:13 + R15:8 + R16:17 + R17:11 + R18:10 = 59 grün.

**Commit:** `e8767e5 feat(audit-round-18): backfill-transcripts endpoint + downgrade cap + tests` auf neuem branch `round-18-recovery-and-downgrade`. PR-URL: https://github.com/haskallalk-eng/voice-agent-phonbot/pull/new/round-18-recovery-and-downgrade

**R19-Backlog** (in [[Phonbot/ZuTun]]):
- billing MEDIUM-4 (syncSubscription SELECT-then-UPDATE race ohne tx — Codex D1)
- auth HIGH-1 BroadcastChannel cross-tab refresh — Frontend-Refactor: BEIDE api.ts UND auth.tsx-Bootstrap müssen channelled werden, nicht nur api.ts (Codex Plan-Review D)
- calendar MEDIUM-2/4 (MEDIUM-1 ist eigentlich schon atomar via Unique-Index, per Codex Plan-Review E)
- backfill-transcripts: org-scoped advisory_xact_lock (Codex A2 LOW), `truncated/maxBatchesHit`-flag (A3 NIT), ORG_NOT_FOUND vs NO_INDUSTRY_CONFIGURED disambiguation (A1 LOW)

### Phonbot — Audit Round 19 (Backfill-Polish + syncSubscription tx)

Codex Plan-Review empfahl R19 = backfill-transcripts polish (A1+A2+A3 aus R18-Review) + syncSubscription FOR UPDATE wrap (billing MEDIUM-4). Calendar + BroadcastChannel ausdrücklich nach R20 verschoben.

**Implementiert (4 Items):**

1. **A1 ORG_NOT_FOUND disambiguation** in [insights.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/insights.ts) — EXISTS-check vor industry-derive. Ein typo'd orgId kriegt jetzt klar 404 ORG_NOT_FOUND statt misleading NO_INDUSTRY_CONFIGURED.

2. **A2 pg_try_advisory_lock auf dedicated client** — Codex Round-19 Code-Review fand BLOCKER in meinem ersten Wurf: lock via pool.query, unlock via pool.query — verschiedene backend connections, unlock no-op auf B, original A behält lock bis idleTimeoutMillis (30s). Fix: `lockClient = await pool.connect()` so dass try_advisory_lock + alle batch UPDATEs + unlock alle die same connection nutzen. Outer try/finally garantiert release auch bei innerem unlock-fehler. Returns 423 LOCKED (RFC 4918) bei contention.

3. **A3 truncated/maxBatchesHit flag** — surface MAX_BATCHES-hit im response body + log.warn so callers können „more work to do" detecten ohne race-anfälligen `transcriptsUpdated < candidates`-vergleich.

4. **billing MEDIUM-4 syncSubscription SELECT FOR UPDATE in tx** in [billing.ts](C:/Users/pc105/.openclaw/workspace/voice-agent-saas/apps/api/src/billing.ts) — pre-UPDATE SELECT + UPDATE jetzt in einem `client.connect()`-tx mit BEGIN/SELECT FOR UPDATE/UPDATE/COMMIT. Vorher konnte ein webhook+sofort-sync-race beide den gleichen period_end lesen → beide resetMinutes=true → second wipt minutes_used nochmal nach erstem legitimen reset. FOR UPDATE serialisiert: zweiter wartet auf ersten COMMIT, sieht fresh period_end, skipt redundant reset.

**Codex Code-Review:** 1 BLOCKER (advisory_lock connection-share) + 4 LOW + 1 MEDIUM (test-fixture brittleness — R20-backlog). BLOCKER vor commit gefixt.

**Tests:** R18-Tests adapted (BEGIN/COMMIT + EXISTS/lock fixtures), 3 neue R19-Cases (404 ORG_NOT_FOUND, 423 LOCKED, lock+release contract mit mockRelease assertions). 19/19 backfill + 11/11 webhook = 30/30 grün.

**Verifikation:** typecheck clean, 153/155 (2 pre-existing). Audit-Tests R14:13 + R15:8 + R16:17 + R17:11 + R18:10 + R19:8 = 67 grün insgesamt.

**INDEX.md:** billing MEDIUM-1 (R18) + MEDIUM-4 (R19) als GEFIXT markiert in 2026-04-26 audit ledger.

**Commit:** `5fdb14a feat(audit-round-19): backfill-transcripts polish + syncSubscription tx` auf neuem branch `round-19-polish-and-tx`. PR-URL: https://github.com/haskallalk-eng/voice-agent-phonbot/pull/new/round-19-polish-and-tx

**R20-Backlog:** auth HIGH-1 BroadcastChannel (Frontend), calendar MEDIUM-2 (silent-skip bei broken connections), calendar MEDIUM-4 (multi-provider eventId arbitrary), ticket HIGH-2 (PII Email — UX-Konsens), Test-fixture builder für pool/client split (Codex R19 review E), composite (org_id, id) Index nach prod measurement, RETELL_TOOL_AUTH_SECRET promotion.

### Phonbot — Prod-Supabase-Migration + DPA-Vollkomplettierung 11/11

Compliance-Session, parallel zu den Audit-Rounds R18/R19 oben. Folgeschritte aus dem 2026-05-01 Cleanup auf Einzelunternehmer-Naming.

**Ehrlicher Fix einer falschen Annahme:** vor 2 Tagen hatte ich gesagt „prod nutzt eigene Postgres, Supabase ist nur dev". Falsch — `dbproxy`-Container leitet via socat-Tunnel zu Supabase weiter. Aktueller prod war auf altem Supabase-Account `vhuvfykdvpypxhsiueay`, neuer war nur in lokaler `.env`. Heute ehrlich migriert.

**Implementiert:**

1. **Prod-Supabase-Migration auf neuen Account `jnqpkkzuveoeipsborwp`** — Lokale Postgres-URL (Passwort + Username + Database) via SSH-stdin-Pipe an prod transferiert (Werte nicht im Chat-Log). Auf prod: `dbproxy`-Container via `docker rm + docker run` mit neuer IPv6 von `db.jnqpkkzuveoeipsborwp.supabase.co` neu gestartet. Erst-Versuch mit `--network phonbot_default` failed (ECONNREFUSED von API-Container) — `--network host` mit Bind auf `0.0.0.0:5432` brachte Connection durch. Neue prod-DB hat 39 Tabellen via frischer `migrate()`. Smoke-Tests live: `/health`, `/avv/`, `/sub-processors/` alle 200, DB-Connection OK.

2. **6 weitere DPAs auf 11/11 komplett** — Supabase via DocuSign (Customer-Entity „Mindrails", später re-sign nötig), Twilio + OpenAI + Stripe + Cartesia + ElevenLabs by-incorporation (PDF/Markdown-Snapshots als Compliance-Nachweis), Sentry via DocuSign (Customer-Entity „Mindrails"). DPA-Modi-Klarstellung: 5 by-incorporation, 4 DocuSign-signed, 1 deutsche AVV (IONOS), 1 by-Service-Nutzung (Cartesia). PDFs lokal + auf prod unter `compliance/dpas/<anbieter>/`, gitignored.

3. **ElevenLabs-DPA-Modus-Korrektur** — initial fälschlich als „muss aktiv unterzeichnet werden" eingeordnet, weil DPA-Volltext keinen by-incorporation-Satz hat. User-Nachfrage „sicher dass es so ist" hat mich auf die ElevenLabs-ToS aufmerksam gemacht: dort steht „Our Data Processing Addendum, which governs our processing..." → eindeutig by-incorporation. Korrigiert + Markdown-Snapshot abgelegt.

4. **Re-Sign-TODO klargestellt** für 4 DocuSign-DPAs (Retell/Resend/Sentry/Supabase): alle mit Customer-Naming „Mindrails" gesigned, das ist keine eingetragene Entity. Nicht akut blockierend (Email-Bindung über `haskalla.lk@gmail.com` identifiziert eindeutig), aber sollten nach UG-Eintragung neu signed werden (Szenario A) oder zu „Hans Waier (Einzelunternehmer)" updated (Szenario B).

5. **Memory: feedback_direct_links_for_actions.md** — User-Wunsch nach immer direkt klickbaren Markdown-Links zu spezifischen Settings-Pages bei jeder Aufforderung („klick X" / „aktivere Y"). Nie nur Pfad-Beschreibung („Login → Settings → Sidebar → Tab").

**Commits (master + push erfolgreich):**
- `2622dc0` OpenAI-DPA — 8/11
- `7046366` (Push-Bridge zwischen den Edits)
- `d5469a6` Stripe + Sentry — 10/11
- `cbe967f` ElevenLabs — 11/11 KOMPLETT

**Offen (R20-Polish + User-Action):**
- Cartesia Zero Data Retention Toggle aktivieren — Toggle aktuell nicht im Dashboard auffindbar (Cartesia-Doku-Suche läuft, evtl. ist's API-Header statt Account-Setting)
- Alten Supabase-Account `vhuvfykdvpypxhsiueay` löschen sobald Migration verifiziert (siehe [Direct-Link](https://supabase.com/dashboard/project/vhuvfykdvpypxhsiueay/settings/general))
- Steuernummer ins Impressum sobald vom Finanzamt erhalten
- 4 DocuSign-DPAs re-sign-TODO (low-priority, bei UG-Eintragung)
- `dbproxy` in `docker-compose.yml` einpflegen für reboot-Persistenz (manueller Container bleibt sonst bei `docker compose down`)

## 🔗 Links
- Gestern: [[Daily/2026-05-01]]
- [[Phonbot/ZuTun]]
- [[Phonbot/Overview]]
